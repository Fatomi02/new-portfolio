## The problem

Zeta was the final project of the Sycamore internship training programme — a
fintech application built to put the whole programme into practice at once,
rather than as a set of disconnected exercises.

{/* TODO: what does Zeta actually do? This is the most important gap on the
     page. A sentence or two on the specifics:

     - Is it payments, savings, lending, budgeting, wallets, transfers?
     - Who is the user, and what can they do in it?
     - Was the brief given to you, or did you choose what to build? */}

## What I built

{/* TODO: the substance. Some questions worth answering:

     - Which screens or flows did you build — onboarding, dashboard,
       transaction history, transfers?
     - Did you build it alone or in a team? If a team, what did you own?
     - Where does the data come from — a real API, a mock backend, Firebase?
     - Does it handle authentication? Fintech reviewers always look for this.

     Screenshots carry a lot here. Drop them in public/images/projects/ and
     reference them like this:

     ![The Zeta dashboard](/images/projects/zeta-dashboard.png) */}

## A problem worth showing

{/* TODO: money apps always have at least one genuinely hard frontend
     problem. Pick the one you hit and show the code:

     - Formatting and validating currency without floating-point rounding
       errors
     - Keeping a balance consistent while a transfer is in flight
     - Guarding against a double-submitted payment
     - Handling a failed or pending transaction in the UI honestly

     A short snippet showing the shape of your solution is worth more than a
     paragraph describing it. */}

## Outcome

{/* TODO: how did the project land? An assessment result, feedback from the
     Sycamore trainers, or simply that you finished and deployed a working
     fintech app at the end of the programme — that is a real outcome, say
     it plainly. */}

## What I'd do differently

{/* TODO: one honest paragraph. You have shipped production work at two
     companies since building this, so you almost certainly see things in it
     now that you would do differently. That contrast is worth writing down —
     it shows growth, which is exactly what an interviewer is listening for. */}
